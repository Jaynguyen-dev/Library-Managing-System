# IMPLEMENTATION RULES

- Use JavaScript only
- Use Express + Prisma + Microsoft SQL Server
- Use React + Vite + Tailwind
- Never introduce TypeScript
- Never change API response shapes
- Keep controllers thin
- Put business logic in services
- Follow implementation phases strictly
- Never rename modules from specification
- Use Microsoft SQL Server only
- Prisma provider must be sqlserver
- Never switch to MySQL
- Never introduce PostgreSQL